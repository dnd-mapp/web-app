import type { Language } from '@ngx-translate/core';

/**
 * The locale every area writes its texts in, and the one the templates are written against. It is the locale in use
 * and the fallback both, since it is the one an area is guaranteed to ship complete.
 */
export const sourceLocale: Language = 'en-US';
